import type { Metadata } from "next"
import AppContent from "@/components/app/AppContent"

export const metadata: Metadata = {
  title: "Create Animation - MotionDrop",
}

export default function AppPage() {
  return <AppContent />
}
